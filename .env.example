# Set this to your Server4 origin when available.
SERVER4_URL=

# MongoDB Atlas connection (never commit the real value).
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/
MONGODB_DB=splitly
